export const metadata = {
  title: "UX Design Critique",
  description: "Multi-agent AI design review panel",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
